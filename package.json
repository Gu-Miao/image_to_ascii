{
  "name": "greedy_snake",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "concurrently \"nodemon --config nodemon.json\" \"vite\"",
    "build:www": "tsc && vite build",
    "build:wasm": "wasm-pack build -t web",
    "preview": "vite preview"
  },
  "dependencies": {
    "image_to_ascii_wasm": "workspace:*"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "less": "^4.1.3",
    "nodemon": "^2.0.20",
    "typescript": "^4.9.4",
    "vite": "^4.0.4"
  }
}
